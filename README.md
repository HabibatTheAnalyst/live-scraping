venv is used so all packages downloaded for a specific project doesnt conflit with another project
so create and activate a new venv for every new project
python3 -m venv venv   and source venv/bin/activate   
pip freeze > requirements.txt  
which python      
rm -rf venv   
create a folder on the home directory ~ using mkdir/projects   or  mkdir ~/projects   
then move the folder created in DEsktop into the projects folder using mv ~/Desktop/live-scraping ~/projects/    
requests work best with static pages 
use selenium if figures changes